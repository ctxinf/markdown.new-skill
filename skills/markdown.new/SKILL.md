---
name: markdown.new
description: Quickly access a page through `https://markdown.new/<target_url>`.
---

Access a static resource page through `https://markdown.new/<url>`.

Example:
```sh
curl https://markdown.new/https://github.com/ctxinf/markdown.new-skill
```

This returns the repository homepage as static, structured Markdown.
